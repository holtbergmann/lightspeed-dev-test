Assumes:
yarn is installed on your machine

To Run the Program:
1. cd to ReactJs in a command prompt
2. execute the command: 'yarn start'
3. Open a browser to http://localhost:8080/parsecsv.html


To Run tests:
1. cd to ReactJs in a command prompt
2. execute the command: 'yarn test'
